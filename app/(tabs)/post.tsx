import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function PostScreen() {
  return (
    <PlaceholderScreen
      title="Drop lore"
      note="The full posting form (vibe tags, tag friends) comes in a later phase."
    />
  );
}
