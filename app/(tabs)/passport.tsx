import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function PassportScreen() {
  return (
    <PlaceholderScreen
      title="Passport"
      note="Check-ins and stamps come later — this phase is foundation + auth only."
    />
  );
}
