import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export default function ExploreScreen() {
  return (
    <PlaceholderScreen
      title="Explore"
      note="Search and filters come later — this phase is foundation + auth only."
    />
  );
}
